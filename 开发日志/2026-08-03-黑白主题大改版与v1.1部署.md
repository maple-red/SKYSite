# 2026-08-03 黑白主题大改版 + v1.1.0 部署

> 今日里程碑：全站从「红白主题」升级为「黑白主题」，完成 v1.1.0 并成功部署到 Linux 服务器。

---

## 一、视觉主题全面升级（黑白主题）

- 全站配色从极简白 + 品牌红 → **黑白灰** 主题
- 品牌色/红色系元素（按钮、卡片、导航 hover、光晕、粒子）全部收敛为黑白
- 各页面顶部背景图替换为**黑白灰度图**（picsum grayscale，每页一张）

## 二、欢迎页：雨夜天气系统

- Canvas 动态天气：**雨丝随风摆动 + 落地涟漪 + 随机闪电（分叉+全屏闪光+雷声震屏）+ 夜空云团 + 云缝星星 + 风线**
- 完整天气循环：暴风雨 → 雨势渐弱 → 雨停星现 → 风起云聚（科学依据：雨大无星、雨停星现）
- 视觉灰度化（背景/雨/云/闪电/光晕全黑白）
- 自定义发光圆环准星光标

## 三、首页改造

- **Hero**：黑白背景图（cover 铺满）、深色底 + 白色字体、可点击的向下箭头（平滑滚动到内容区）
- **紧凑栏**：下滑后 SKYL1N 抬高，下方显示**当前时间（含年月日+星期）/ 实时天气（wttr.in 英文）/ 总访客 / 在线**
- **近期经历**：项目 + 校园经历混排（经历在前），滚动时**左右交替滑入**
- 修复：粒子画布挡 hover 的 pointer-events bug

## 四、各页面更新

- **关于**：新文案（养牛坡职业技术学院·大四·Java）、诗句问候语、经历区块上移贴合、SKYL1N 卡片加图标、颜文字
- **作品集**：每个项目自动配图（picsum）、分类标签去图标
- **空间**：发表说说用首页头像、点赞改文字、颜文字、删空状态文案
- **留言板**：颜文字
- **简历**：精简到**一页**（删实习/爱好区块、压缩排版、技能 6 项）

## 五、联系卡重构

- 点击后在**右侧滑出**白色不透明卡片（3s 无操作自动收回）
- 加回联系方式图标（邮件/电话/链接）
- 修复 inset 限制导致卡片内容被挤的问题

## 六、站点信息

- 网站名改为 **「SKYL1N的基地」**
- favicon：**TL 颜文字**（T 形双眼 + L 形微笑嘴 + 轨道白点）
- 全站装饰 emoji 清除（保留功能性 ✕），页面加颜文字点缀

## 七、部署

- 打包 `skyl1n-test1.0.2.tar.gz`（1.5MB）
- Linux Docker Compose 部署成功（前后端 + MySQL）
- **访客统计 0/0 修复验证通过**（`crypto.randomUUID` 非安全上下文降级，`http://IP` 访问正常）
- favicon 缓存问题：给 `<link rel="icon">` 加 `?v=` 版本号解决

### 7.1 SSL / HTTPS 正式证书部署

- **证书**：linyou.cloud 正式证书（`cert.pem` 证书链 + `key.pem` 私钥）已配置
- **nginx**：80 → 301 跳 https；443 正式证书主服务；支持 www 子域名
- **Docker**：frontend 容器挂载证书目录（`./frontend/certs:/etc/nginx/ssl`）
- **部署**：`skyl1n-test1.0.3.tar.gz` → 服务器 3 容器 Up
- **访问**：`https://linyou.cloud` 可访问，显示锁图标（正式证书生效）
- **防火墙**：iptables + 腾讯云防火墙放行 22/80/443
- **注意事项**：
  - `http://linyou.cloud` 被拦「未备案」（ICP 备案审核中，通过后正常）
  - IP 访问有证书警告（证书绑定域名，用域名访问即可）
  - 详情见 `SSL-HTTPS部署文档.md`

## 八、待办

- **移动端适配**（详见 `未解决问题.md` 第 3 节）：紧凑栏移动端不显示、联系卡超屏、欢迎页 canvas 性能等
- 后端 Maven 依赖下载慢（服务器网络），可后续加阿里云镜像加速
- 前端 bundle 超 500KB（Element Plus 全量），可后续 code-split
